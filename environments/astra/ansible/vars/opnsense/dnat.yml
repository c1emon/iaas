---
# Placeholder for hand-written OPNsense Destination NAT desired state.
#
# DNAT management is intentionally not implemented yet because the current
# oxlorg.opnsense collection used by this repository does not provide a stable
# dedicated Destination NAT / port-forward module.
#
# Keep this file as the future review location for DNAT declarations. The
# placeholder playbook loads and validates only that opnsense_dnat_rules is a
# sequence, then fails before any OPNsense API write.
#
# Do not copy generated exports directly into this file as apply input. When a
# native module becomes available, define and review a concrete schema before
# enabling writes.
#
# Draft future shape, not currently consumed for writes:
# opnsense_dnat_rules:
#   - description: Example HTTPS port forward managed from Git
#     interface: wan
#     ip_protocol: inet
#     protocol: tcp
#     destination: wan_address
#     destination_port: 443
#     target: 192.0.2.10
#     target_port: 443
#     enabled: true
#     state: present
opnsense_dnat_rules: []
