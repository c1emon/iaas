# Project Rules

1. This project uses `uv` to manage the Python environment. Run Python scripts and commands with `uv run`.
2. Before implementing any OpenSpec change, switch to the implementation branch.
