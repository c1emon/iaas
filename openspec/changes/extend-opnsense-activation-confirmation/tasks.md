## 1. 能力核定

- [ ] 1.1 按固定 Collection 和明确目标 OPNsense 版本核定现有只读接口及同步返回，形成按操作分组的支持/缺口说明；验证每个支持结论有源码或官方接口依据，并列出未知/不支持会阻断的操作，不访问设备或增加权限。

## 2. 候选合同与写前准入

- [ ] 2.1 实现 v2 候选的确认合同、配置派生内容动作、缓存策略及有界等待参数，保留 v1 request；用合同测试验证摘要绑定、只读 plan、blocked 候选、普通 no-change、activation_recovery 无独立刷新语义及 apply 不追加动作，并验证保存成功/内容失败后再次计划的原生激活恢复与人工处理两种出口。
- [ ] 2.2 在首写前复核所有阶段的必要确认能力、权限、已知读取上限及缓存适用条件，并沿用相关漂移和绑定 pending-change 检查；验证末阶段能力缺失亦导致零写入，可选检查缺失不阻塞，缓存失效只返回重新计划。
- [ ] 2.3 实现 candidate/result/recovery 分立版本及旧恢复材料的受限读取；验证旧候选 apply/verify 明确拒绝、v1 request 仍可用、恢复需已核清后态、不能从旧激活提升结果继承完成证据。

## 3. 等待、判定与部分失败

- [ ] 3.1 实现阶段总期限、次数和单请求界限下的固定只读轮询；用可控时钟及合成响应验证延迟完成、明确失败、超时/取消未知，轮询不重复写入、重载或内容刷新。
- [ ] 3.2 取消 active=verified 的通用激活提升，实现按操作证据组合及分离的内容更新/活动结果；验证刷新成功且成员相同、刷新失败但旧内容匹配的成对用例，以及批内部分对象成功不能掩盖必要失败。
- [ ] 3.3 保留阶段/资源结果、写前配置和已核清后态，将独立 verify 接到同一只读核对逻辑；验证无历史动作证据时按当前状态汇总退出码、附加 unsupported 与真实 not_applicable 的区别、不可读/截断不能当不适用，以及 verify 不追认/改写原 apply 结果；回归冲突停止、部分保存后停止后续写入、不自动重试/回滚，私有输出失败仍保留恢复材料。

## 4. Alias 机制适配

- [ ] 4.1 调整静态 host/network 的完整成员核对与独立动作完成判定；验证 IPv4/IPv6、仅描述变化、陈旧观察/截断拒绝以及成员匹配不再直接提升激活。
- [ ] 4.2 在 1.1 核定的能力内实现 port Alias 的相关已加载规则展开检查；验证协议、源/目标端口和范围，以及无消费者时 not_applicable、不创建验证规则。
- [ ] 4.3 实现静态 networkgroup 的有效成员计算与活动比较；验证必要现场依赖、未选中期望值不替代现场、动态/不完整依赖不按空集通过，依赖读取不扩大写入。
- [ ] 4.4 在已核定能力内实现动态 Alias 初始化、更新、允许的缓存复用和加载判定；验证新增、重新启用允许/不允许缓存、来源变化拒绝旧缓存、合法空与失败区分、只改描述不强制刷新及完整读取边界；验证来源 B 已保存但获取失败后，只有已核定且候选披露的原生激活内容行为可恢复，否则明确 manual_required/显式配置逆向恢复指引，不伪造差异、不循环提示重新计划。
- [ ] 4.5 按原生语义实现禁用/删除的活动退役核对；验证对象缺失、空表、残留表及不可读各自结果，仅在必要时要求表消失且不清 PF state、不删除未选择对象。

## 5. Gateway 与接口组

- [ ] 5.1 在已核定能力内实现 PBR Gateway 按字段/用途的必要运行状态核对；验证接口/下一跳、必要路由/活动消费者及已启用监控配置，不强制默认路由、ping、启用监控或创建消费者。
- [ ] 5.2 在已核定能力内实现接口组注册、成员和 filter reload 的独立判定；验证注册成功但 reload 失败、成员不符、相关规则接口匹配及无消费者场景。

## 6. 集成与交付

- [ ] 6.1 运行受影响 workflow/reader/writer/contracts/runtime 的 Python 测试、Ansible 检查及既有 local/DinD 集成测试；验证普通 no-change、activation_recovery、Filter/NAT/VIP 同步确认、恢复及凭据隔离无回归，不新增全版本矩阵。
- [ ] 6.2 更新通用手册、合成候选/结果示例、版本迁移说明及软件验证记录；逐项核对 AC-01–AC-10 覆盖，列明已支持与仍阻塞的操作，不将测试替身描述成设备验收。
- [ ] 6.3 完成严格 OpenSpec 校验、git diff --check 和提交前 GitNexus 图变更分析；确认 change 的已完成范围与测试一致。分次交付时保留未完成任务，不因记录能力缺口而标记资源实现完成。

实施约束：本清单全部为后续任务；apply 前须按仓库规则确认对应实现分支。任务 4/5 缺少必要 API 时保留未完成并报告具体缺口；SSH、插件、补丁、新权限、发布、调用方接入及真实设备验收均不由本清单自动授权。
