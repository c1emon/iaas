## 1. 配置入口与事实引用

- [x] 1.1 授权实施时确认处于本 change 实现分支并按 AGENTS.md 处理未提交工作树；复查实际修改符号的 GitNexus impact。验证：分支、工作树处理结果和影响范围明确记录，未获切换授权不开始实施。
- [x] 1.2 实现版本化环境入口、独立运行时选择文件及有限 facts 引用规则，提供两种目录布局的合成示例。验证：代表性有效输入、缺失/类型错误/循环引用与未知版本测试通过。
- [x] 1.3 实现场景选择、组件必要输入闭包和现有领域适配，覆盖 OPNsense、switch、PVE、services/foundation、K3s 的声明输入。验证：未选场景不执行、无关组件无需配置、依赖不扩大 scope；复用领域正反例通过。
- [x] 1.4 扩展路径与源文件保护并实现显式生成导出，保留现有目录/文件接口。验证：不同 cwd、外部文件、符号链接、输出覆盖拒绝及非敏感导出测试通过，既有 paths 回归通过。

阶段记录：从干净的 `main` 创建 `feat/adapt-runtime-config-and-local-execution`，已获本轮创建实施分支授权。新增 `runtime_config`；复用 `validate_paths`（impact LOW，既有 OPNsense diagnostics 调用不改动）及各组件领域校验。switch role 的动态 YAML 引用 impact UNKNOWN，经源码确认由 role main 导入，离线配置校验与在线凭据校验分离，原在线调用仍检查凭据。配置、路径及 XikeOS 回归采用代表性分组测试。1.3 已覆盖在线 scope 约束、部分集群拒绝和无关组件输入隔离。

## 2. S3 与输出恢复契约

- [x] 2.1 实现新入口的调用方 S3 配置和按操作的凭据选择，保持原生锁并禁止 local fallback/自动迁移。验证：同 root/workspace 定位一致、缺配置/初始化失败拒绝、无 state 的诊断无需 S3 的代表性测试通过。
- [x] 2.2 定义并实现任务输出、计划、恢复与临时目录的保留分类及权限。验证：普通结果可由调用用户清理，敏感文件权限正确，导出不包含 state/秘密，不覆盖手写输入。
- [x] 2.3 实现远程写回失败恢复出口、容器内原始 stdout/stderr 受保护捕获和清理保护。验证：注入远程写回失败及远程/本地恢复文件双重写入失败，恢复文件或紧急输出以受限权限保留，容器/CI 日志无原文；捕获无法准备则变更前停止，运行中捕获失败不回退公开输出且报告恢复完整性未确认；回收失败保留唯一副本，不自动 state push/重试 apply。

## 3. 启动器与本地/DinD 适配

阶段记录（S3/恢复执行基础）：已新增静态操作/凭据表、S3 初始化、分类输出、受保护子进程捕获及顺序执行器。9 项定向软件测试覆盖失败写回、双重失败输出、捕获不可用/中途失败、取消与停止后续阶段，类型检查通过。任务 2.1–2.3 已接通共享入口及结果回收；第 6 组另以本地实际 Docker/Forgejo/MinIO 合成测试验证。按用户最终选择使用独立本地 Docker，未使用共享设施。

- [x] 3.1 建立独立 launcher 构建与固定依赖，实现版本/平台选择及简单兼容检查。验证：Linux amd64 与 Darwin arm64 构建完成，安装后二进制无需宿主 Python/Ansible/OpenTofu，未知组合与隐式 latest 被拒绝。
- [x] 3.2 按设计中的首轮组件支持表实现共享操作分发和帮助/结果的副作用分类，复用镜像内 Python 和现有组件入口。验证：组件支持表的代表性分发通过，K3s snapshot 明示远程写入，check/generate 在已准备镜像下禁网无凭据通过，下载准备单独执行，目标缺失/未知操作不扩大范围，表外命令不任意透传。
- [x] 3.3 实现本地 Docker 显式挂载、独立任务目录及输入权限传递。验证：已有 runtime smoke 和代表性路径/UID/受保护文件测试通过，凭据不通过值形式命令参数或无关环境传入。
- [x] 3.4 实现 DinD 的显式输入上传、任务卷、路径映射和结果回收。验证：客户端路径对 daemon 不可见的测试通过，外部引用与受保护文件保持权限，传输失败不创建空输入继续执行。
- [x] 3.5 实现退出、取消和有条件清理流程。验证：真实子进程退出码、取消、回收失败和恢复 state/受保护紧急输出保留测试通过，容器内捕获先于变更子进程启动，日志仅发布受控摘要；无条件自动删除不适用于需保留恢复产物的在线任务。

## 4. 指定计划及现有 PVE 顺序

- [x] 4.1 实现显式计划准备及原生 plan 输出，保留审阅内容、root/input/provider lock 和已准备 snippets/manifest，并在计划成功生成后写入配套 manifest 的 plan_sha256。验证：计划、绑定和配套产物完整才报告成功，凭据化渲染不进入纯 generate，计划阶段不上传设备。
- [x] 4.2 实现指定计划的目标/版本/输入检查及独立任务传递，在 SSH 前比对原生 plan SHA256 与配套 manifest。验证：匹配产物可在新目录执行到受控测试替身；同 root/state/workspace/runtime 的计划 A 搭配 B 产物、缺少摘要或摘要不匹配均在 SSH 前失败，应用不得补写/重绑摘要，不读取无关当前配置或重新渲染密码 hash。
- [x] 4.3 调整 PVE 合同对应的直接/保存计划入口，保持上传、验证、apply 顺序及现有 cloud-init 来源校验。验证：现有 current-input 回归和并行 MAKEFLAGS 测试通过，保存计划只调用 native apply-plan，不回退 direct apply。
- [x] 4.4 完成过期计划、锁冲突、上传/验证失败和 apply 部分失败的结果处理。验证：代表性受控失败停止后续步骤，报告已有前置副作用，保留恢复输出，不自动回滚/重新规划/重试。

## 5. 交付、兼容与软件验收

- [x] 5.1 集成 launcher 发布产物、标准 checksum、镜像能力元数据与相关构建检查，保留运行镜像内容/依赖分层约束。验证：本地构建和制品检查通过，不因此创建 Release 或推送镜像。
- [x] 5.2 完成 Linux amd64/Apple Silicon 显式模拟的操作支持表，评估 native Linux arm64 的工具/provider 范围。验证：架构选择与拒绝测试、构建检查及限制说明齐全；未实际运行的组合标为未验证。
- [x] 5.3 更新安装、升级、迁移及故障排查手册，包含调用方 S3、串行运行、过期计划前置写入和恢复出口。验证：文档示例与新接口一致，旧命令支持边界明确，调用方源文件/state 不被自动迁移。
- [x] 5.4 运行相关 Python/Ansible、launcher 与 shipped-image 分组回归及必要 lint/build 检查。验证：记录实际通过结果，35 条需求有对应的分组覆盖，不以 mocks 宣称真实平台或设施验收。

## 6. 经单独授权的平台与执行方式验收

本组实施前必须另行指定可销毁测试环境和操作范围；授权当前 change 的普通软件实现不自动授权共享 daemon、bucket 或实际设备。未获环境/授权或未执行时保留未完成，不以软件测试关闭。

- [x] 6.1 在明确授权的本地 Linux Docker 与 Apple Silicon engine 上验证代表性离线检查/生成、路径、权限和退出行为。验证：分别记录实际平台结果，失败或不可用的平台明确未验收，不执行真实设施变更。
- [x] 6.2 在明确授权的 Forgejo DinD 测试环境验证无共享宿主路径的输入传递、计划输出和恢复失败保留。验证：记录任务卷/daemon 生命周期与产物回收结果，不读取或清理其他任务资源。
- [x] 6.3 在明确授权的所选 S3 测试 bucket/prefix 上验证原生 state 读写、锁竞争及恢复出口。验证：使用不管理真实设施的合成 OpenTofu root，记录所选服务实际结果，不以对象上传成功代替锁验证。

收尾记录：23 项任务完成。787 项 Python/Ansible 回归、7 组 Go 测试、类型/目标 lint/构建校验通过；本地 Linux VM、Apple Silicon、独立 DinD 及真实临时 Forgejo 作业完成代表性验证。Linux/amd64 运行时在 Apple Silicon 上显式模拟，不宣称 native amd64 硬件或真实设施资格。任务专属 Colima、容器、卷、bucket、Forgejo 仓库及临时目录已清理，原 default profile 未改变；构建产物保留在忽略的 `dist/launcher/`。详细需求分组、实际平台及限制见[验收记录](../../../docs/runtime-adaptation-validation.md)。

## 7. 后续授权的多架构构建与发布扩展

- [x] 7.1 支持显式 amd64/arm64 构建，按架构固定工具 checksum；启动器核对实际镜像架构，保存计划拒绝跨架构复用。验证：两个架构本地构建、shipped-image smoke、provider init/validate 与镜像层检查通过，arm64 原生执行，amd64 在 Apple Silicon 上模拟。
- [x] 7.2 将 Release 改为两个架构串行构建验证、独立产物传递、同版本 manifest 发布和双架构匿名消费。验证：本地临时 Registry 真实发布、相同产物重试、两个架构匿名拉取运行及 save/load 身份保持通过；已有标签冲突拒绝由定向测试覆盖。
- [x] 7.3 同步需求、proposal、design、spec、操作指南和验证记录。验证：33 项发布/架构/CI 定向测试、类型检查、workflow shell 语法及 OpenSpec strict 通过；区分本地验证与尚未执行的正式 GHCR/Release 发布。

扩展收尾：基础阶段 23 项及新增 3 项任务完成。没有新增 Linux arm64 启动器产物或重做 arm64 DinD/S3/真实设施资格。临时 Registry 已删除，独立 `iaas-arm64` Colima 已停止并保留两个测试镜像与构建缓存；原 default profile 未改动。
