## 1. Configuration contract

- [x] 1.1 Add paired synthetic VM-inventory and K3s-overlay fixtures covering template-derived architecture, cluster-wide node-network role, server, agent, bootstrap, embedded-etcd, endpoint, CIDR, component-policy, architecture-keyed pinned artifact/acquisition proxy, registry mirror/fallback/TLS, service-proxy lifecycle, snapshot source, token reference, and secret-reference intent; verify fixtures contain no real Astra nodes, copied per-node facts, interfaces, or secret values.
- [x] 1.2 Add required canonical `amd64` architecture to the generic PVE template model, migrate existing template declarations from their pinned image/template evidence, inherit it into every normalized VM, and regenerate `pve_architecture` in Ansible hostvars; verify missing, alias, and unsupported template values fail, unchanged fields preserve their meanings, no K3s field enters VM inventory, and no PVE operation runs.
- [x] 1.3 Implement a compositional K3s loader and validator that resolves VM-owned architecture and SSH facts plus the unique NIC, IP/prefix, and network matching the declared cluster-wide node-network role from the selected generated Ansible inventory; verify missing/ambiguous role matches, forbidden storage/ingress selection, automatic fallback, per-node overrides, and attempted VM-fact overrides fail.
- [x] 1.4 Validate architecture-keyed pinned artifact/checksum coverage, embedded-etcd topology, one-or-odd-three-plus server counts, exactly one initial server, unique derived node IPs, bootstrap-derived or stable external endpoint policy with automatic TLS-SAN derivation, CIDR syntax and overlap, server-wide component policy, action-scoped acquisition path, version-supported registry fallback enforcement, verified registry TLS, bounded derived proxy bypasses, snapshot-source eligibility, stable server-token reference, and external secret references; verify focused positive and negative tests pass.
- [x] 1.5 Add deterministic redacted review rendering for the composed non-secret model; verify snapshot tests prove stable derived output and secret-like input rejection.

## 2. Read-only preflight

- [ ] 2.1 Add reusable read-only preflight tasks for Debian support; deterministic `x86_64` -> `amd64` guest normalization and agreement with generated `pve_architecture`; cgroups; kernel capabilities; time; runtime interface ownership of the derived node IP; ports; disk; configured APT-source reachability; same-path selected-architecture artifact reachability; authenticated registry reachability and TLS-file validation; and conflicting install state. Verify unsupported/mismatched VM facts and missing action-scoped credentials are rejected, and tests reject `apt update`, configuration writes, global proxy changes, and other mutation-capable modules or commands in the preflight path.
- [ ] 2.2 Add an explicitly scoped K3s preflight playbook that consumes only the validated composed model and selected inventory hosts; verify syntax, lint, no-host, unreachable-host, warning, and blocking-failure behavior.

## 3. Controlled deployment

- [ ] 3.1 Add a K3s prerequisite role separate from `vm_baseline`; verify idempotent tasks cover only declared Debian K3s host prerequisites.
- [ ] 3.2 Add pinned K3s artifact acquisition using the same resolved endpoint and action-scoped proxy/authentication path as preflight, with integrity verification and no unpinned remote installer execution; verify wrong versions, missing checksums/secrets, path mismatch, and checksum mismatch fail before installation.
- [ ] 3.3 Add server and agent configuration roles using the node-network-role-derived IP and server advertise address, automatically derived fixed-endpoint TLS SANs, separate external-CNI and packaged-component policy, restrictive secret-file permissions, and redacted task output; verify rendered-config and secret-leak tests pass.
- [ ] 3.4 Render K3s/containerd `registries.yaml`, fallback-deny K3s setting, verified TLS files, and server/agent systemd proxy environment before first service start; derive bounded loopback/node/cluster-domain/API/registry `NO_PROXY`, inject authentication at runtime, and verify mirror failure cannot reach upstream when denied, unchanged policy causes no restart, and changed or retired policy uses health-gated serial restart while removing only role-owned files.
- [ ] 3.5 Add ordered embedded-etcd deployment playbooks for the explicit `cluster-init` server, additional servers, and agents; after initial self-signed-CA bootstrap, read the active secure token through a protected ephemeral `no_log` path, compare its credential portion with the external short token (or also validate CA hash for an external secure token), and use only the active secure token for later joins. Verify short/secure equivalence, CA-hash mismatch, no persistence/output, unsupported topology, and later-phase rejection after prerequisite/bootstrap/join failure.
- [ ] 3.6 Require an explicit non-empty mutation scope with no implicit all-host default or silent dependency expansion, and rerun preflight in the same invocation for the same composed model and scope; verify missing, mismatched, or unresolved-prerequisite scopes fail before mutation.
- [ ] 3.7 Add repeat-run behavior checks for managed configuration, artifacts, and services; verify an already converged synthetic host reports no unnecessary reinstall or restart.

## 4. Verification and day-two operations

- [ ] 4.1 Add read-only bootstrap verification for API reachability, declared node registration, role, version, embedded-etcd health, and core service state; verify only expected CNI-not-initialized status is tolerated and all other readiness failures remain blocking.
- [ ] 4.2 Add an explicit embedded-etcd snapshot workflow targeting the single configured source server; verify server/etcd health, token-free output, deterministic naming, root-only node-local storage, no automatic deletion, and no controller/repository copy or restore-readiness claim.
- [ ] 4.3 Add a resumable serial whole-cluster controlled-upgrade workflow that rejects partial/implicit scopes, downgrade, skipped-minor, and unsupported mixed-version transitions; reruns same-model/scope preflight; creates a same-invocation snapshot; health-verifies and skips nodes already at the exact target; upgrades remaining servers then agents one at a time; and verifies API/etcd or node/service state after each node; verify one representative interruption/retry path and stop-on-failure behavior.
- [ ] 4.4 Keep restore, snapshot retention/deletion, uninstall, datastore replacement, and destructive node removal absent; verify the first-version command surface exposes no executable path and documentation points to separate recovery design.

## 5. Operator surface and documentation

- [ ] 5.1 Add separate offline-safe, online read-only, and mutation-capable K3s command entrypoints requiring explicit overlay and inventory paths; require an explicit non-empty scope and action-scoped runtime secrets needed by each online path, verify missing inputs fail before host access, and keep aggregate `make check` offline-only.
- [ ] 5.2 Document the source-inventory generation branches, template-clone/cloud-init handoff, VM baseline, composed K3s intent, same-path artifact acquisition, registry fallback/TLS and service-proxy retirement, the separate APT/shell/Git boundary, command safety classes, protected secret injection, stable server-token retention, resumable upgrade, external-CNI intermediate state, and software-only acceptance boundary; verify current docs no longer mark already implemented VM prerequisites as missing.
- [ ] 5.3 Keep Astra composition and later platform layers absent; verify no real K3s nodes, Cilium, Flux, CSI, Gateway, application resources, or environment runtime secrets are added.

## 6. Validation and review

- [ ] 6.1 Run focused Python and Ansible tests, syntax checks, lint, type checks, YAML checks, deterministic-render checks, and representative runtime secret-channel/output redaction/no-diff tests through the shared secret mechanism; verify secrets never enter CLI arguments, generated/cached state, Ansible facts/fact cache, diffs, ordinary temporary files, or logs, and verify all capability tests pass using synthetic inputs only.
- [ ] 6.2 Run the complete repository offline gate and secret scan; verify no offline command contacts or mutates PVE, guests, foundation services, or K3s and no snapshot-like artifact enters generated or repository paths.
- [ ] 6.3 Strictly validate the OpenSpec change and review requirement-to-test coverage; verify every scenario has implementation evidence within the authorized software-only scope.
- [ ] 6.4 Run complete GitNexus impact and change analysis before editing shared PVE symbols and before commit, resolve any partial, truncated, UNKNOWN, HIGH, or CRITICAL result, and verify the generic PVE diff is limited to the additive template/VM architecture fact and generated hostvar.
