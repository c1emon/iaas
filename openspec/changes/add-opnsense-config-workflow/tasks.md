## 1. 实施准入与能力核对

- [ ] 1.1 获得实施授权后检查工作树并按仓库规则确认实现分支；刷新 GitNexus，对实际修改的校验/分派符号做 impact，记录风险和受影响调用路径。本次设计不代表此任务已执行。
- [ ] 1.2 核对固定 Collection 的七类配置读取、分页/身份/字段、引用与激活结果能力；用固定源码和代表性响应形成简短支持说明，尤其验证 Groups 返回 ok 的证据边界，必需能力不足明确阻塞，不自行扩展依赖或资源类型。

## 2. 通用选择与读取

- [ ] 2.1 定义版本化 request/candidate/result/recovery 文件合同及操作准入，复用现有 files/options；以未知字段、单设备目标、类别/身份选择、显式空集合和非本站输入测试验证。
- [ ] 2.2 实现七类标准资源的有界只读适配及字段规范化，复用固定提供者；分组测试分页不完整、缺失字段、重复匹配、默认值和未知状态，确认没有保存、reload 或 state 清理。
- [ ] 2.3 实现 plan 的新增/更新/显式删除/no-change/unknown 差异和必要引用读取；验证原生顺序/地址族/端口/gateway 变化可见、计数器被忽略、动态 Alias 内容不混作配置漂移。

## 3. 候选与执行准入

- [ ] 3.1 输出自包含候选及单文件摘要，绑定完整标准声明、对象集合、目标连接标识、runtime digest/架构及实际来源；验证原始 inputs 改动不改变已有候选，候选摘要/目标/runtime 不符在写前拒绝。
- [ ] 3.2 用选中变更和必要现场观测构建有效状态，检查支持的正反向引用并产生有界阶段；测试创建→引用切换→退役，以及未选择引用切换、引用环、缺失依赖时拒绝且不扩大写入。
- [ ] 3.3 加入整个候选写前相关漂移检查和每阶段依赖检查；测试已有对象修改、新增身份被占用、未选中引用变化与读取未知时无首个写入，阶段失败后不继续。
- [ ] 3.4 实现共享激活冲突/unknown/调用方检查结论的独立准入，结论绑定本次目标、候选及执行；测试无结论拒绝、已检测冲突不可被结论覆盖、串行化边界和阶段外部变化停止。

## 4. 资源执行与验证

- [ ] 4.1 复用并按需拆分现有资源 playbook/batch 任务，执行候选固定集合与阶段；验证本地和加载后校验先于凭据/写入、未选中对象保留、每阶段固定激活、check-mode/no-op 及旧直接入口兼容。
- [ ] 4.2 输出保存/激活/配置/活动核对的独立结果，区分 accepted 与 confirmed；用保存后激活失败、Groups 仅返回 ok、超时和部分保存测试验证停止依赖阶段、不自动重试或回滚。
- [ ] 4.3 实现独立 verify 及 apply 后自动配置核对，按实际能力读取活动态；测试 absent 确认、配置不符、活动未支持/未确认和 completed_with_unverified 结果，不将业务验收标为完成。
- [ ] 4.4 实现候选显式 activation_recovery 的 no-change 激活路径；验证仍受新候选审查、漂移和共享激活准入约束，普通无变化不 reload。

## 5. 恢复、输出与 launcher

- [ ] 5.1 写前保存受影响对象实际旧值、不存在标记及恢复可表达性，并在同一 recovery 文件记录执行身份、尝试结果与可确认后态；测试现场值不同于历史声明、后态更新失败为 unknown、无法无损恢复模式标 manual_required、私有输出准备失败则不写设备。
- [ ] 5.2 在 plan 中从 recovery 准备新的反向候选，再由 apply 显式执行；测试新增删除、旧对象重建、后续漂移/未知原结果必须核清和审查、manual_required 不被自动恢复。
- [ ] 5.3 接入 launcher 的 read/plan/apply/verify、文件选择、effects、私有输出和 local/DinD 映射；复用现有执行层测试，验证离线无凭据、在线无 S3 依赖、禁止 OP_* 传递、结果采集失败保留材料。

## 6. 集成与交接

- [ ] 6.1 以两种合成目录布局完成代表性集成，按合同覆盖 IPv4/IPv6/混合 Alias、七类资源差异、未受管保留及失败路径；运行相关测试、lint/build、strict OpenSpec 和 GitNexus 变更分析，不建立穷举矩阵或访问现场。
- [ ] 6.2 更新通用 OPNsense、runtime 配置及 launcher 手册，替换旧 launcher 禁止说明，提供 request/candidate/result 与恢复示例；通过链接、合同及命令一致性检查确认无本站默认值、无调用方策略/基线逻辑。
- [ ] 6.3 写简短软件验收和消费交接说明，列明能力、未验证活动项、版本接入前提与恢复限制；明确发布、调用方接入和现场业务验收尚未执行，不以软件结果替代它们。
