## 1. 实现归属调整

- [x] 1.1 后续 apply 前检查分支和工作树，协调 infra-ops 承接专用生成器、fixtures、策略测试和专用文档，再从 iaas 移出这些实现及运行时 selective-proxy 分支；完成依据为双方交接清单和 iaas 不再提供高层分流入口。不得自动丢弃既有未提交内容。
- [x] 1.2 将标准规则和 Alias 输入合同及通用说明改为接受调用方手写或生成声明，复用已有源文件选择入口；用普通生成别名和规则样例验证身份、状态、依赖及入口一致，保留未经整理的设备导出不能直接部署的限制，不依赖 infra-ops 或特定生成器。同步主 specs 时一并修正 Alias Purpose 的手写限定；Gateway/VIP 本次继续引用原基础源。

## 2. 通用校验修正

- [x] 2.1 补齐 source/destination 单目标反向匹配约束；通过直接资源正反例验证单目标可进入正常校验、多目标在凭据前失败，并保留其他字段约束。
- [x] 2.2 完成通用接口网络/别名上下文及已有入口保护的静态覆盖判断；验证覆盖、缺失、混合静态/动态成员、地址族不全及所选声明冲突，保持既有保守保护和外部引用语义，不增加策略例外。
- [x] 2.3 统一离线和 Ansible 加载后的规则校验，必要复验放在凭据前；在适配边界处理带元数据的合法基础类型，用真实本地 Ansible 加载测试及字符串/bool/int 反例验证，不弱化通用类型要求。

## 3. 通用接入与回归

- [x] 3.1 更新通用操作/运行时说明，移除 iaas 负责分流编译、分类与迁移阶段生成的描述；验证标准生成文件可经既有校验及 Ansible 源路径接入，明确保存、激活、外部引用与调用方授权边界。
- [x] 3.2 运行 uv 管理的 OPNsense 校验、别名依赖、运行时配置和直接 Ansible 入口相关回归及必要语法/类型检查；完成依据为修订后源码的实际结果，旧 169 项通过记录不作本任务证据。
- [x] 3.3 执行 strict OpenSpec 校验和适用仓库检查，记录通用合同版本及是否已发布；完成依据为可审查的软件状态与交付记录，不以本地测试或镜像构建代替已发布版本和现场验收。
