## 1. 分类契约与读取

- [ ] 1.1 核对固定支持版本的原生 Alias／Group／Gateway 及现有规则来源证据，形成最小适配规则和代表性脱敏夹具；验证无证据项保持未知，不按站点名称推断。
- [ ] 1.2 增加独立的来源、管理能力、依据／来源关联和必要数据性质，保留配置转换失败；用 internal／用户 external、动态 Alias、预置可编辑规则、派生项及标记冲突代表例验证。
- [ ] 1.3 保留完整内部枚举、最小系统引用描述与枚举来源／范围；验证无标准配置但有可靠角色证据的系统 Alias／组可用于支持的规则引用，地址／端口类型不符及证据不足仍拒绝，反向引用、传递依赖与循环保护不被绕过。

## 2. 展示与兼容性

- [ ] 2.1 接入 read 专用 `include_system` 和本地 `--include-system`，分离完整观察产物与默认视图；验证默认隐藏条件、显式对象详情、严格布尔及非 read 拒绝行为。
- [ ] 2.2 增加枚举／选择／展示／隐藏数量及分类、表达失败、未知摘要；验证选择和分页不完整时的计数与状态，以及开关不扩大读取范围。
- [ ] 2.3 更新 candidate／result／recovery 版本和严格契约校验；以同组代表断言验证 runtime／local 的 v3 结果及完整观察／投影标记，旧版本与缺分类材料拒绝，request v1、声明和 `read_only` 操作语义保持不变。

## 3. 计划、执行与恢复保护

- [ ] 3.1 将独立管理能力接入现存目标的计划与接管准入，保留普通创建路径和所有权要求；验证局部覆盖对象默认可见但不获得独立操作权限，只读／来源管理／未知目标阻断，未选中无关对象不额外阻断。
- [ ] 3.2 将必要分类及最小引用描述绑定候选和执行重读检查；验证管理能力、相关来源或引用角色变化在写入前阻断，仅 origin 标签修正不误阻断，展示开关不改变候选、依赖或漂移结论。
- [ ] 3.3 将管理能力约束接入恢复和激活恢复；验证非独立派生项不获得反向候选、不自动选中来源，用户对象新建逆删除和删除逆创建可行，新身份占用阻断，正常创建后分类回读不误报漂移、证据不足仍停止后续变更。

## 4. 验证与文档

- [ ] 4.1 使用项目 uv 环境运行受影响的 reader／conversion／workflow／runtime 测试和现有类型／质量校验，验证普通资源计划、执行、验证与恢复代表路径；记录离线覆盖边界。
- [ ] 4.2 更新读取开关、分类解释、未知处理、材料迁移和 infra-ops 消费说明，验证示例与结果契约一致，不宣称完整 PF 或设备／业务验收。
- [ ] 4.3 运行 OpenSpec 严格校验、检查最终差异并完成 GitNexus 图变更分析；核对仅触及授权范围，提交前确认无敏感原始响应或额外设备操作。
