## 1. Ownership and contract

- [x] 1.1 Update the repository-layering contract, `README.md`, `docs/README.md`, `docs/k3s-foundation-platform-design.md`, and `platform/README.md` so shared in-cluster platform desired state belongs to an external platform repository, while ordinary application releases remain application-owned; keep the directly updated canonical `iaas-repository-layering` Purpose synchronized, and verify current non-historical documents contain no old in-repository platform ownership claim, platform implementation root, or compatibility alias.
- [x] 1.2 Add synthetic platform-handoff intent and expected-bundle fixtures with an explicit secure Git repository URL, revision, normalized relative path, and external bootstrap-credential reference; verify they contain no real Astra endpoint, VM/node reference, token, kubeconfig, private key, repository credential, or Kubernetes manifest.
- [x] 1.3 Add fail-closed handoff composition that derives cluster name, K3s version, a normalized HTTPS API endpoint on port 6443, and explicit whole-cluster scope from the validated K3s model while emitting only a `whole-cluster` scope class; accept only `https`/`ssh` Git URL schemes and verify hostname/IPv4/IPv6 endpoint normalization, copied VM/PVE/K3s facts, embedded Git credentials, URL query/fragment secrets, HTTPS user information, implicit repository defaults, unsafe paths, plaintext secrets, unrecognized secret-reference syntax, and partial scopes fail.

## 2. Readiness and handoff artifact

- [x] 2.1 Add an infrastructure-read-only handoff workflow that reruns existing K3s verification for the same model and whole-cluster scope, reads `/var/lib/rancher/k3s/server/tls/server-ca.crt` from the declared initial server through the existing authenticated guest path, requires one X.509 CA certificate, validates the API endpoint TLS/name against it, and renders its DER SHA-256 as `sha256:<lowercase-hex>`; fail before output for unreachable, mismatched, unverified, or unexpected CNI/readiness conditions, and prove an endpoint-observed-only fingerprint is rejected.
- [x] 2.2 Render the deterministic handoff bundle only after readiness succeeds, to an explicit operator path with restrictive permissions and atomic replacement; reject committed environment, generated inventory, and other repository-owned output paths, and verify the bundle contains only derived cluster identity/trust metadata, repository coordinates, the external credential reference, and an explicit platform-unqualified status while ordinary stdout contains only its path and non-sensitive identity.
- [x] 2.3 Add safety tests proving the IaaS workflow validates only recognized external-reference syntax, never contacts a secret provider or resolves bootstrap access, and never places credential material in CLI arguments, handoff output, inventory, Ansible facts/fact cache, generated environment state, diffs, temporary files, or logs; also prove it does not install Flux or apply Kubernetes resources.

## 3. Operator boundary

- [x] 3.1 Add separate entrypoints: offline `platform-handoff-check` requires K3s intent, generated inventory, platform-handoff intent, and explicit whole-cluster scope but no output path; `platform-handoff-render` requires those inputs plus an explicit output path and is infrastructure-read-only with a local protected-artifact write. Verify missing inputs fail before host access and the aggregate `make check` remains offline-only.
- [x] 3.2 Document explicit operator delivery of the bundle to the platform workflow and platform-side consumption: require an independently revocable temporary grant, resolve it externally, compare its normalized API endpoint and CA identity with the bundle, install Flux from the platform repository, verify first reconciliation, and invalidate the grant's cluster-side authentication; state that deleting only the secret-provider value is insufficient and that transport/publication/signing, CI triggering, and all consumer steps are not executed or qualified by this repository.
- [x] 3.3 Verify no Cilium, Flux, CSI, Gateway, certificate, observability, application, cross-repository CI trigger, credential creation, or credential revocation implementation is introduced.

## 4. Validation

- [x] 4.1 Run focused deterministic-render, invalid-input, forbidden-output-path, ordinary-stdout, Ansible syntax/lint, redaction, restrictive-permission, and no-mutation tests using synthetic inputs only.
- [x] 4.2 Run the complete repository offline gate and secret scan; verify no command contacts PVE, guests, K3s, Git providers, secret providers, or a platform repository.
- [x] 4.3 Strictly validate the OpenSpec change, review requirement-to-test coverage, and run GitNexus change analysis before commit.
