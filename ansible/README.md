# Ansible

Ansible inventories, playbooks, roles, and collection requirements will be added here after host groups and secret handling are defined.

Do not commit plaintext vault passwords, private keys, or environment-specific secrets.
