---
all:
  children:
    opnsense:
      hosts:
        opnsense-core:
          ansible_connection: local
          ansible_host: localhost
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
          opnsense_api_host: 10.1.0.254
